# ThyroCare AI API — Phase 5 runtime dependencies
# Pinned to versions verified on Python 3.14 (cp314 wheels).
# Recommended runtime: Python 3.11+. Python 3.12 used in Dockerfile.
# Motor removed — use pymongo.AsyncMongoClient.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
pymongo==4.17.0
httpx==0.28.1
slowapi==0.1.10
